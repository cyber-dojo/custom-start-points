version: 1

trail:
  attestations:
    - name: pull-request
      type: pull_request
  artifacts:
    - name: custom-start-points
      attestations:
        - name: provenance
          type: decision

        - name: sbom
          type: decision

        - name: snyk-container-scan
          type: decision
